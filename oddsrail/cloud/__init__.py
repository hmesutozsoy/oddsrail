"""The hosted oddsrail: the same MCP server behind OAuth accounts, reached
over streamable HTTP (mcp.oddsrail.app/mcp) instead of stdio.

Nothing here touches money. Accounts exist so that every signed-in agent
gets its own paper ledger and so that a listing in a connector directory
has someone to be accountable to. See oddsrail.hosted for what the profile
removes and why.
"""
